{
  "version": 2,
  "builds": [
    {
      "src": "docker-compose.yml",
      "use": "@vercel/docker"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "http://localhost:8000/$1"
    }
  ]
}